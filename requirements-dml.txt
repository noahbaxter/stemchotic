# DirectML variant (AMD/Intel GPUs on Windows), chosen in the launcher
# questionnaire. The [dml] extra pulls onnxruntime-directml + torch_directml.
audio-separator[dml]==0.44.2
static-ffmpeg==3.0  # bundled ffmpeg/ffprobe on PATH (see requirements.txt)
-e ./libs/chotic-ui
